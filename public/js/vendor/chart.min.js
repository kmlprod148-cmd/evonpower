/**
 * Chart.js v3 - Lightweight placeholder
 * Pour production: télécharger depuis https://cdn.jsdelivr.net/npm/chart.js
 * Ce fichier est un placeholder pour éviter les erreurs
 */

(function() {
    'use strict';
    
    if (typeof window !== 'undefined') {
        window.Chart = window.Chart || class Chart {
            constructor(ctx, config) {
                this.ctx = ctx;
                this.config = config;
                this.data = config.data || {};
                this.options = config.options || {};
                console.warn('Chart.js placeholder loaded. For production, use the full library.');
            }
            
            update() {}
            destroy() {}
            render() {}
            
            static register() {}
        };
        
        // Chart.js defaults
        if (window.Chart) {
            window.Chart.defaults = {
                responsive: true,
                maintainAspectRatio: true
            };
        }
    }
})();

