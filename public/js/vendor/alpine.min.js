/**
 * Alpine.js v3 - Lightweight placeholder
 * Pour production: télécharger depuis https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js
 * Ce fichier est un placeholder pour éviter les erreurs
 */

(function() {
    'use strict';
    
    // Alpine.js minimal implementation
    if (typeof window !== 'undefined') {
        window.Alpine = window.Alpine || {
            start: function() {
                console.warn('Alpine.js placeholder loaded. For production, use the full library.');
            },
            data: function(name, callback) {
                return callback;
            },
            store: function(name, data) {
                return data;
            }
        };
        
        // Auto-start
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', () => window.Alpine.start());
        } else {
            window.Alpine.start();
        }
    }
})();

