/**
 * Three.js r128 - Lightweight placeholder
 * Pour production: télécharger depuis https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js
 * Ce fichier est un placeholder pour éviter les erreurs
 */

// Three.js core exports
if (typeof window !== 'undefined') {
    window.THREE = window.THREE || {
        Scene: class Scene {},
        PerspectiveCamera: class PerspectiveCamera {},
        WebGLRenderer: class WebGLRenderer {
            constructor() {
                this.domElement = document.createElement('canvas');
            }
            setSize() {}
            render() {}
            setClearColor() {}
        },
        AmbientLight: class AmbientLight {},
        DirectionalLight: class DirectionalLight {},
        Mesh: class Mesh {},
        MeshLambertMaterial: class MeshLambertMaterial {},
        CylinderGeometry: class CylinderGeometry {},
        BoxGeometry: class BoxGeometry {},
        Box3: class Box3 {
            setFromObject() { return this; }
            getCenter() { return { x: 0, y: 0, z: 0 }; }
            getSize() { return { x: 1, y: 1, z: 1 }; }
        },
        Vector3: class Vector3 {
            constructor(x = 0, y = 0, z = 0) {
                this.x = x;
                this.y = y;
                this.z = z;
            }
            sub() { return this; }
        },
        GLTFLoader: class GLTFLoader {
            load(url, onLoad, onProgress, onError) {
                if (onError) {
                    setTimeout(() => onError(new Error('GLTFLoader placeholder')), 100);
                }
            }
        },
        sRGBEncoding: 'sRGBEncoding'
    };
    
    console.warn('Three.js placeholder loaded. For production, use the full library.');
}

